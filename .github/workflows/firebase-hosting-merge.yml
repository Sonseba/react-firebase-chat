# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        env:
          secrets: inherit
          VITE_API_KEY: ${{ secrets.VITE_API_KEY }}
      - name: Create dotenv file
        run: |
          touch .env
          echo VITE_API_KEY = $VITE_API_KEY >> .env
          echo length: ${#VITRE_API_Key}"

      - name: display
        run: |
          echo "$VITE_API_KEY" |  awk '$1=$1' FS= OFS=" "
          
          touch .env
          echo VITE_API_KEY = $VITE_API_KEY >> .env
          
          cat .env | awk '$1=$1' FS= OFS=" "
      - run: npm ci && npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_CHATAPP_DC4BF }}
          channelId: live
          projectId: chatapp-dc4bf


